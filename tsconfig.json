{
  "compilerOptions": {
    "target": "ES2017",
    "lib": ["dom", "dom.iterable", "esnext"],
    "allowJs": true,
    "skipLibCheck": true,
    "strict": true,
    "noEmit": true,
    "esModuleInterop": true,
    "module": "esnext",
    "moduleResolution": "bundler",
    "resolveJsonModule": true,
    "isolatedModules": true,
    "jsx": "react-jsx",
    "incremental": true,
    "plugins": [
      {
        "name": "next"
      }
    ],
    "paths": {
      "@/*": ["./src/*"]
    }
  },
  "include": [
    "next-env.d.ts",
    "**/*.ts",
    "**/*.tsx",
    ".next/types/**/*.ts",
    ".next/dev/types/**/*.ts",
    "**/*.mts"
  ],
  "// exclude: ios-shell": "The Capacitor shell is a SEPARATE npm project with its own package.json, and Vercel only installs this one's dependencies. Without this line `capacitor.config.ts` is part of the program, its `import type { CapacitorConfig } from \"@capacitor/cli\"` cannot resolve on Vercel, and `next build` dies at Running TypeScript. It passes locally only because ios-shell/node_modules exists on the dev machine, which is why this got past the recipe.",
  "exclude": ["node_modules", "migration", "e2e", "playwright.config.ts", "ios-shell", "store-shots"]
}
